TICKET H1 — Hotspot layer (hits.js): labels must not cover their object, evasion in short frames, onWeg cleanup hook.

TASK
The owner reports (2026-09-04, verbatim): "Bij veel objecten als de speelmand staat de tekst over het object" — on many objects such as the speelmand the hotspot's icon+word button sits on top of the object itself and hides it. Also, on landscape phones the layer runs out of room and hotel buttons land on a running game's card and hooks. Fix the placement policy of demos/dierenhotel/hits.js and add the cleanup hook the shell needs.

EXPECTED OUTCOME
1. Placement: a hotspot attached to an object (mik box from world.js: `volg()` returns the object's screen box) is placed so that the object stays visible: default anchor directly ABOVE the object's box (button bottom edge at the box top minus a small gap, horizontally centred), with the existing evasion (UITWIJK) only moving it further up or sideways; when the top of the frame is too close, place it BELOW the box; never centred over the object unless the object is smaller than the button and the ticket-holder (the game) asks for it explicitly via an option (e.g. `op: 'midden'`). Keep the current behaviour for hotspots that are not attached to an object (free positions, cards, chips) and for the getalTag number tags, which are meant to sit on the object — but make sure a getalTag never hides more than the object's top face (offset it to the object's upper edge if it does not already). Survey every existing hotspot definition (games/*.js, hotel.js, econ.js: search for Hits.plaats / ctx.hits and hotspot: {obj: ...}) and list in your report which ones change position and whether any of them relied on the old centred placement (the speelmand button in hotel.js, the bel, the prikbord, the voerbakjes, the tobbe, the bedden, the sleutelhaken, the meubels vakken, the kassa).
2. Short frames: while a game owns a `vast` card (a sum card with prio), hotspots of other owners (bel, prikbord, other games' buttons, doors) that would overlap the card or the game's own hotspots are hidden (or collapsed to a small icon at the frame edge, ≥ 44 px, only if it stays tappable) instead of overlapping; they come back when the card is removed. Evidence of the problem: /home/pc/Documents/xnw/rkn/.fanout/scratch/dierenhotel/sleutels/x1-sleutels-740x360-na.png (Boek and Prikbord on the card/hooks).
3. `onWeg` hook: Hits.weg and Hits.wisEigenaar call an optional `onWeg` callback stored on the hotspot record (a shell ticket sets it from ui.js's somkaart to tear down its resize listeners); document it in the hits.js header.
4. The display:none measuring fix from commit 710003e and the 16-per-layer cap stay. No per-frame allocations added in the placement path (the layer already relayouts on demand).
5. Probe .fanout/scratch/dierenhotel/hits-plaats.js (Playwright, port 420×860, land 860×420, 740×360 and 360×740; require('/home/pc/work/ergomouse/node_modules/playwright')): for every hotspot with an object in receptie, gang, kamer1, keuken, tuin, zwembad and wasserij, assert the button's box does not intersect more than 15 % of the object's box (read the mik box via the same function the layer uses, expose a debug getter if needed), all targets ≥ 48 px (44 on < 360 px), no two tappables overlapping; open the sleutels game at 740×360 and 844×390 and assert zero overlapping tappables and the card not covering the hooks; screenshots hits-<room>-<viewport>.png; ≥ 40 assertions; final line in the alles.sh format; add the row `hits-plaats|node hits-plaats.js` to alles.sh (Edit tool, atomic, keep every existing row).
6. Regressions against your worktree: hotspots.js, w1 (port + land), loop (port + land), regels, smoke, sleutels/test.js (port + land), voerkar, tobbe (port + land), bedden, meubels — report every number; if an assertion encodes the old centred placement, say so with numbers and propose the replacement, do not loosen silently.

CONTEXT
Repo /home/pc/Documents/xnw/rkn; isolated git worktree branched from main (harness-created; absolute paths). Vanilla JS, offline file://. HOTEL.md §9 rules are binding. The scratch dir is gitignored: copy harnesses into your worktree's scratch (DH_SRC / DH_OUT where supported) and copy your probe, screenshots and the alles.sh row to /home/pc/Documents/xnw/rkn/.fanout/scratch/dierenhotel/ at the end. A parallel ticket M1b edits world.js and art.js (canvas, resize bus); if you need a change in world.js (e.g. the mik box exposing the object's top), implement it in hits.js from what volg() already returns, or report it as a concern. Kill only browsers you started. Long shell heredocs are rejected: use Write/Edit. No dependencies, no agents, no commit. Write .fanout/specs/api-h1.md (placement rule, `op` option, onWeg hook, short-frame policy).

MUST NOT
Edit world.js, art.js, ui.js, style.css, hotel.js, econ.js, any game file, rooms.js, state.js, registry.js. Do not change hotspot ids, the Hits API signatures (additive options only) or the 16 cap.

WRITE SET
demos/dierenhotel/hits.js, .fanout/scratch/dierenhotel/hits-plaats.js, .fanout/scratch/dierenhotel/alles.sh (one row), .fanout/specs/api-h1.md.

OUTPUT FORMAT
First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED; then ≤ 25 lines: the placement rule (file:line), the list of hotspots whose position changed, probe numbers per viewport, regression numbers, screenshots, worktree path/branch, concerns.
