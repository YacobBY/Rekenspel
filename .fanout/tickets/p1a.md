TICKET P1a — Two new rooms (zwembad, wasserij) and reserved zones in the tuin for the Dierenhotel demo.

TASK
Add the rooms `zwembad` and `wasserij` to demos/dierenhotel/rooms.js and wire them into the map, the room bar and the voerkar rest spots; reserve two disjoint floor zones in the tuin. This prepares five upcoming minigames specified in /home/pc/Documents/xnw/rkn/.fanout/specs/dierenhotel-golf3-games.md (read G1 zwembad, G3 hinkelpad, G4 wasmandtoren, G5 souvenirkraam; you implement none of the game logic).

EXPECTED OUTCOME
1. rooms.js RUIMTES: `zwembad` (icon 🏊, Dutch name "Zwembad") reached by a door from `tuin` with the reciprocal door on the tuin side; `wasserij` (icon 🧺, "Wasserij") with a door from `keuken` and its reciprocal. Sizes chosen to work with the per-room scale of commit 710003e (read HOTEL.md §1 and world.js maatVan first): zwembad long along x, suggestion 120×80 voxels; wasserij about 100×90. Same schema as the other rooms (w, d, wand, vloer, deuren, decor, slots, loop factor); `slots` empty; `decor` only door frames plus at most two fixed props (e.g. a ladder/stair at the pool edge, a laundry shelf).
2. Zwembad floor: a rectangular pool inset in the floor, water-blue via Rooms.vloerKleur (or an equivalent floor-colour rule), with a tiled deck around it; expose `Rooms.get('zwembad').bad = {x0, x1, z0, z1}` in voxels; the pool must span most of the room's x extent so a swim of L metres maps linearly to x. Guests must be able to stand on the deck at the start edge (x0 side) and at the far edge; the wander grid (`vrij`) must exclude the water so idle guests never stroll into the pool (check how `vrij`/`plekken` are built, rooms.js ~455-475 at 25a4b52).
3. Tuin: `zones: {hinkel: {x0,x1,z0,z1}, kraam: {x0,x1,z0,z1}}` reserving two disjoint floor areas (hinkel along the back edge, long enough for ~11 stepping stones; kraam along a side edge, room for a stall plus a guest in front) that stay clear of existing tuin decor footprints and of the doors; document the values in a comment.
4. hotel.js: a `KAART` cell for each new room (grid at hotel.js ~42-43; missing rooms fall off the map). world.js: `RUST` entries (~876) for both rooms so the voerkar has a parking spot.
5. HOTEL.md §1: room list updated (one sentence each).
6. Suite .fanout/scratch/dierenhotel/p1a.js (port 420×860 and land 860×420, ≥30 assertions): both rooms reachable via Rooms.pad from receptie and via the room bar chips; map cells unique and inside the grid; `bad` inside the room and ≥ 80 % of r.w wide; no wander spot inside the water; tuin zones disjoint, inside the floor, clear of decor and doors; frame fill ≥ 60 % and no clipping of the room box in both new rooms (reuse k1.js checks); no console errors; screenshots p1a-zwembad-{420x860,860x420}.png, p1a-wasserij-{420x860,860x420}.png, p1a-tuin-zones-420x860.png (zones drawn as a translucent overlay by the test only). Existing suites smoke, l1, k1, loop (port+land), hotspots must stay green — run them against your worktree (check each suite's URL; several hard-code the main tree path — override locally, do not change their defaults).

CONTEXT
Repo /home/pc/Documents/xnw/rkn; you work in an isolated git worktree branched from main at af8ab32 (rooms already 1.5× with per-room scale; guests sleep on beds; games/registry.js has a ctx extension hook via window.CTX_UITBREIDINGEN — do not edit registry.js). Vanilla JS, offline, no build. Playwright at /home/pc/work/ergomouse/node_modules/playwright; harness patterns in .fanout/scratch/dierenhotel/ (k1.js, slaap.js, tobbe/spel.js). .fanout/scratch is gitignored: write your suite under your worktree's .fanout/scratch/dierenhotel/ pointing at YOUR worktree's index.html and copy it plus screenshots to /home/pc/Documents/xnw/rkn/.fanout/scratch/dierenhotel/ at the end. Document the new room data (`bad`, `zones`, ids, doors) in .fanout/specs/api-p1a.md inside your worktree; do not edit GAMES-API.md. Hard rules: HOTEL.md §9; frozen math in state.js untouched. No new dependencies; no blanket pkill; you do not spawn agents; do not commit. Parallel tickets edit other regions of rooms.js (MODEL map / registerModel), world.js (Dier class, drawing/cache) and hotel.js (wish code) in their own worktrees: keep your hunks localized (RUIMTES table + vloerKleur + the two one-line map/RUST additions) so the merges stay clean.

CONSTRAINTS / MUST NOT
Do not change existing rooms' dimensions, camera code, any game, wishes, art.js, state.js. Do not add voxel models beyond simple boxes already available in MODEL (a new model hook is another ticket).

WRITE SET
demos/dierenhotel/rooms.js (RUIMTES table, vloerKleur, zones/bad), demos/dierenhotel/hotel.js (KAART only), demos/dierenhotel/world.js (RUST only), HOTEL.md §1, .fanout/specs/api-p1a.md, scratch suite + screenshots.

OUTPUT FORMAT
First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED; then ≤30 lines: room dims and door positions, `bad` and `zones` values, file:line hunks, suite numbers (p1a and the existing ones), screenshot paths, worktree path/branch, concerns.