TICKET M1c — Hotel flow and the five existing games on phones (touch, short frames, text floors, layout bus).

TASK
The shell (M1a: keypad strip in short frames, compact landscape chrome, touch semantics, audio unlock), the canvas layer (M1b: World.onKader layout bus, stable frame, density) and the hotspot layer (H1: buttons above their object, short-frame hiding, drop catch area) are on main. Bring hotel.js, econ.js and the existing games (voerkar, bedden, sleutels, tobbe, meubels) in line so every flow is playable with a finger on the phone profiles, and extend the device suite with real game steps.

EXPECTED OUTCOME
1. Layout bus: hotel.js (~1043-1050 opMaat: window resize/orientationchange → paintCheckin), games/bedden.js (~739-741) and any other window resize/orientationchange listener in the write set subscribe to World.onKader(fn) (unsubscribe in stop()/weg()) instead of adding window listeners; no double relayout on rotation (M1b's probe counts window listeners: target 0 in these files). Before re-creating a card under the same id, call the old card's weg() (paintCheckin ci_som).
2. Landscape check-in: at 844×390, 740×360 and 667×375 the check-in card must not cover the arriving guest (coverage < 10 % as w1 asserts for portrait): place CI_PLEK/CI_HOOG for short frames (card left of the balie, guest visible right) and rely on H1's short-frame policy for the hotel buttons (verify prikbord/deur/bel do not overlap the card; if they still do, hide them explicitly while state.checkin is active).
3. Text floors: child-facing text set inline below 12 px in games (voerkar.js:108 .72rem, tobbe.js:395 .72rem and :398 .7rem, plus any other occurrence you find with a grep for rem/px font sizes in the write set) goes to ≥ 12 px; wording stays.
4. Duplicate tap handlers: voerkar.js karhot (Hits 'aan' + C.sleep onTap, ~771/783) and tobbe.js tb_dier (~862/916) fire twice on a tap; M1a moved the single-fire guard into makeDraggable, so remove the redundant __bronTik guards and the duplicate handlers where they are now dead code, keeping exactly one action per tap (probe it).
5. Rekening (econ.js): the coin purse → toonbank drag works with a finger (CDP touch drag) in both orientations on every profile, including 860×420 with 4 guests where the purse was reported untappable (P1e concern 4): make the purse reachable (position or evasion), assert it.
6. Keypads: every game that shows a keypad (checkin, tobbe, bedden, sleutels, meubels, voerkar) works with padPlek 'auto' in short frames (strip under the frame) — verify each one at 740×360 and 844×390 with a real tap sequence; if a game positions its own elements assuming the pad is inside the frame, adjust it.
7. mobiel.js game steps: extend /home/pc/Documents/xnw/rkn/.fanout/scratch/dierenhotel/mobiel.js (M1a's suite; keep its structure and profile list) with steps that play one round of each game with real gestures on every profile: voerkar (zak → vak drag and the correction chips), tobbe (rek → tobbe drag + keypad), sleutels (sleutel → haak drag), bedden (kist → bedrij drag + keypad), meubels (doos → vak drag), rekening (munt → toonbank). Assertions per step: no console errors, all tappables ≥ 48 px (44 < 360 px), no overlapping tappables, card not covering the active guest, the action actually happened (state changed). Target: all profiles green; report per profile.
8. Regressions against your worktree: every game suite (voerkar port+land, bedden, bedden-layout, sleutels port+land, tobbe port+land+xs, meubels port+land), w1 port+land, loop port+land, hotspots, hits-plaats, regels, smoke, mobiel (all profiles), mobiel-perf. Report every number; if an assertion encodes the old double-handler or window-listener behaviour, name it with numbers and propose the replacement.

CONTEXT
Repo /home/pc/Documents/xnw/rkn; isolated git worktree branched from main (harness-created; absolute paths; fast-forward to main's HEAD if the worktree is older). Read first: .fanout/specs/api-m1a.md (padPlek, opKader helper, Snd unlock), api-m1b.md (World.onKader/hermeet, density), api-h1.md (placement `op`, short-frame policy, catch area), HOTEL.md §9. Vanilla JS, offline file://. The scratch dir is gitignored: copy harnesses into your worktree's scratch (DH_SRC / DH_OUT) and copy mobiel.js and screenshots to the main scratch at the end (alles.sh already has the mobiel rows). Five new game files (games/zwembad.js, wekker.js, hinkel.js, was.js, kraam.js) are being written in parallel by other workers: do not touch them. Kill only browsers you started. Long shell heredocs are rejected: use Write/Edit. No dependencies, no agents, no commit.

MUST NOT
Edit world.js, hits.js, ui.js, style.css, index.html, snd.js, game.js, art.js, rooms.js, state.js, registry.js, or the five new game files. Do not change math, wording or game flows beyond what the items above require.

WRITE SET
demos/dierenhotel/hotel.js, demos/dierenhotel/econ.js, demos/dierenhotel/games/{voerkar,bedden,sleutels,tobbe,meubels}.js, .fanout/scratch/dierenhotel/mobiel.js (+ screenshots), .fanout/specs/api-m1c.md (what games must do on phones: onKader, padPlek, catch area, text floors).

OUTPUT FORMAT
First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED; then ≤ 30 lines: per item file:line with measured numbers, mobiel.js per-profile results, regression numbers, screenshots, worktree path/branch, concerns.
