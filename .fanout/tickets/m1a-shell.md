TICKET M1a — Mobile shell: touch, viewport, chrome and keypad placement for the Dierenhotel demo.

TASK
The owner requires the game to work on real phones (iOS Safari and Android Chrome, portrait and landscape). A read-only scout audited demos/dierenhotel with Playwright device emulation and found the shell (HTML/CSS/ui.js/snd.js/game.js) is the main blocker. Fix the shell and deliver a device-emulation suite for it. A parallel ticket M1b fixes the world canvas (world.js, hits.js, art.js): do not touch those files; where you need a layout signal from the world, use `World.onKader(fn)` if it exists at runtime (M1b adds it: a single debounced callback fed by a ResizeObserver on #world, returning an unsubscribe function) and otherwise fall back to your own window resize/orientationchange listener behind the same small helper, so the code works before and after the merge.

EXPECTED OUTCOME (each item names the scout's evidence; verify it yourself first)
1. Keypad in short frames (P0, ui.js ~299 padBreed): today the in-world keypad becomes a single row only when #world.clientWidth ≥ 660, so on landscape phones (frame height ~200-300 px) a two-row pad (326×118) sits inside the frame and collides with the card and hotspots. New rule: when the world frame is shorter than 300 px, the sum card's keypad is rendered as a strip outside the frame (directly under it, full width, one row of 12 keys ≥ 44×44, 48 where it fits), never inside; the card itself stays small inside the frame. Expose it as an option `padPlek: 'auto' | 'binnen' | 'buiten'` on Ui.somkaart with 'auto' as the default, so existing games need no change.
2. Compact landscape chrome (P0, world.js:491-527 clamps the frame to 200 px because the chrome takes ~256 px): add `@media (orientation: landscape) and (max-height: 450px)` rules in style.css that hide .foot, merge the topbar and rondebalk into one 48 px row (keep every control reachable and ≥ 48 px), and move the kamerbalk to a vertical rail beside the frame (or an overlay chip strip inside it), so the frame can grow to at least 260 px on 844×390 and 740×360. Coordinate: M1b will lift the 200 px clamp to "what fits"; you deliver the chrome that makes room.
3. Viewport units and safe areas (P1): body min-height 100vh → 100dvh with a fallback, .sheet max-height 92dvh, #app paddings using env(safe-area-inset-*) with max() fallbacks, .toast bottom using the inset; overscroll-behavior none on html as well; .overlay overscroll-behavior contain and a body scroll lock while a sheet is open (html.sheet-open{overflow:hidden} toggled in openSheet/closeSheet).
4. Touch semantics (P1): html{touch-action:manipulation}; touch-action none only on drag sources and drop targets (.hot, .hotbron, .coin, .gastkaart and whatever makeDraggable/dragKit attaches to); user-select none and -webkit-touch-callout none on .hot, .ghost, .kchip, .btn, .wtag; contextmenu preventDefault inside makeDraggable; for pointerType 'touch' offset the drag ghost ~40 px above the finger and test the drop point at the ghost anchor (keep the 8 px slop); move the single-fire click guard (today __bronTik in games) into makeDraggable so every dragKit source fires once per tap (do not edit the games; leave their guard in place, it becomes redundant).
5. Toast placement (P1): in max-height 450 px viewports the toast sits at the top under the topbar (or as a wolk inside the frame), pointer-events none, never over the keypad or kamerbalk.
6. Kamerbalk (P1): 8 rooms + plattegrond = 9 chips must be reachable on 360-393 px phones without an invisible scroll: chips with icon above a 12 px word at min-width 40-44 px so they fit in one row, or wrap to two rows ≤ 400 px; keep the plattegrond chip pinned; scroll-snap and edge fades if scrolling remains.
7. Text floors (P1): child-facing text never below 12 px on phones (labels under icons .hot .lbl, .kchip .kn, .somzin, .zeg, .kzk, badges): use clamp(12px, .75rem, 14px) or equivalent; let .somzin wrap to two lines instead of ellipsis (HOTEL.md §9 keeps the ≤ 8 words rule, so two short lines are fine). Do not ship a webfont; note the Comic Sans fallback issue in your report.
8. Audio (P1, snd.js:19-25): no AudioContext creation or sound in startup/render paths (Hotel.start → naarKamer plays Snd.deur at load today: make Snd itself ignore calls until unlocked instead of editing hotel.js); unlock on the first pointerdown/touchend (create + resume + a one-sample silent buffer), schedule sounds only when the context is running (else resume().then(play)), suspend on visibilitychange hidden and resume on the next pointerdown (game.js keeps its save-on-hidden).
9. Somkaart listener leak (P1, ui.js ~460 padOpnieuw): the card's resize listeners survive when hotel.js re-creates a card under the same id; stamp the element with its owning closure, bail out and padUit when it is not ours, and provide an onWeg hook so Hits.weg / wisEigenaar can tear down listeners (M1b adds the Hits side; you add the ui.js side and call it if present).
10. Suite .fanout/scratch/dierenhotel/mobiel.js (Playwright, require('/home/pc/work/ergomouse/node_modules/playwright'), chromium only; WebKit is not installed and must not be installed): profiles devices['iPhone 13'] + landscape, devices['Pixel 5'] + landscape, 375×667 / 667×375 dpr 2, 360×740 / 740×360 dpr 3, all with hasTouch and isMobile; real gestures (page.tap, CDP Input.dispatchTouchEvent for drags, a vertical pan on the canvas and one on a hotspot); steps: start screen, new game, check-in with the keypad (portrait and landscape), open the kassa sheet and close it, rotate mid check-in (setViewportSize) and back, a +80 px height-only change; assertions: zero page errors / console errors / console warnings (the AudioContext warning counts), every visible tappable ≥ 48×48 (44 on < 360 px), no tappable overlapping another, no element outside the viewport, keypad outside the frame when the frame is < 300 px, toast not intersecting keys, kamerbalk chips all inside the viewport, text sizes ≥ 12 px, body not scrollable in portrait when content fits; at least 30 assertions per profile; `node mobiel.js [profile]` runs one profile, no argument runs all; final line in the alles.sh format; add rows `mobiel|node mobiel.js` to alles.sh. Screenshots mobiel-<profile>-<step>.png.
11. Existing suites stay green against your worktree: smoke, w1 (port + land), loop (port + land), regels, hotspots, plugin, the four game suites (bedden, sleutels, tobbe, meubels), voerkar. Report every number; if a suite asserts the old behaviour (e.g. pad inside the frame), say so with the assertion and propose the honest replacement instead of loosening silently.

CONTEXT
Repo /home/pc/Documents/xnw/rkn; you work in an isolated git worktree branched from main (the harness creates it; use absolute paths inside it). Vanilla JS, offline file://, no build. HOTEL.md §9 rules are binding. The scratch dir /home/pc/Documents/xnw/rkn/.fanout/scratch/dierenhotel/ is gitignored: copy harnesses into your worktree's scratch and point them at your worktree (DH_SRC / DH_OUT where supported); copy your finished mobiel.js, alles.sh and screenshots to the main-tree scratch at the end. The scout's raw probe scripts may still exist at /tmp/mobiel-scout.js and /tmp/mobiel-scout-leak.js (use as a starting point if present). Kill only browsers you started. Long shell heredocs are rejected by the sandbox: use the Write/Edit tools. No dependencies, no agents, no commit.

MUST NOT
Edit world.js, hits.js, art.js, hotel.js, econ.js, rooms.js, state.js, registry.js or any game file (report needed changes there as concerns for ticket M1c). Do not change the math, wording or game flows.

WRITE SET
demos/dierenhotel/style.css, demos/dierenhotel/index.html, demos/dierenhotel/ui.js, demos/dierenhotel/snd.js, demos/dierenhotel/game.js, .fanout/scratch/dierenhotel/mobiel.js, .fanout/scratch/dierenhotel/alles.sh (rows only), .fanout/specs/api-m1a.md (padPlek option, onKader fallback helper, Snd unlock contract, CSS hooks — for the docs ticket).

OUTPUT FORMAT
First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED; then ≤ 30 lines: per item file:line and the measured before/after numbers, mobiel.js results per profile, regression suite numbers, screenshots, worktree path/branch, concerns for M1b/M1c.
