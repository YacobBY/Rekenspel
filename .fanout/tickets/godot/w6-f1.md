# W6-F1 — Fix batch for W6 (CI + Pages) after the preliminary review

FINDINGS TO FIX (from R-W3W6; ledger entry of 2026-09-09)
1. BLOCKING tools/probe.js:163,178 — the probe waits for the skeleton's old lines ([probe] knop_paginavlak=, [probe] speel_knop=) that W3 replaced with "[probe] knop <id>=" (see scenes/main.gd:350-355 and .fanout/scratch/godot-w3/probe.js for the lines W3 drives). Rewrite the lifecycle part of the probe against the current shell: boot → start sheet → tap the start button → hotel visible → tap one hotspot button → a card or sheet appears → reload → state persisted. W3 is concurrently fixing the sheets (they currently render as a 560×24 strip) so the reload/persistence step may only pass after W3-F1 lands; run against the live tree at the end and report which steps pass.
2. BLOCKING tools/probe.js:186 — the "one tap = one press" gate matches the boot line "[probe] tik=0" and passes with zero taps. Gate on a tik line emitted after your tap with a value ≥ 1, and fail if no tap was performed.
3. SHOULD-FIX pages.yml:102-106 — deploy from main without running tools/test.sh publishes a red main; make the deploy job need a test job (or reuse test.yml via workflow_call) so a failing suite never publishes.
4. SHOULD-FIX the three OFL licence files: the lead added include_filter="fonts/*.txt" to export_presets.cfg so they are in the pck; additionally copy them beside index.html in the export step (tools/export.sh and pages.yml) so they are readable on the deployed site.
5. Re-run actionlint and shellcheck; re-run every run: step locally as before.

RULES
Same write set and constraints as your original ticket unless extended below; architecture.md may be edited surgically for contract fixes. Other fix workers run concurrently on disjoint files. No commits, no git add, no subagents, no blanket process kills. Before reporting run: import (0 ERROR), tools/test.sh (0 fout, or list exactly which failing tests belong to another workstream still in flight), export (0 ERROR). Report: status word first, then at most 20 lines: per finding "fixed: how" (or "not fixed: why"), check results with counts and exit codes, contract changes, concerns.
