# V1-F1 — Fix batch after the blind verification of wave 1

FINDINGS TO FIX (V1, ledger entry of 2026-09-10)
1. HIGH hotel/rekening.gd:209-214 — the checkout wolk "<naam> gaat naar huis" opens with volg on a guest who is still in another room; the wolk is laid out against a frame it does not fit and renders one letter per line with a ≈22×23 px pill for about 3 s. Fix the class, not the instance: a wolk/card whose anchor is off-frame or in another room must (a) wait for the guest to arrive, or (b) anchor to a fixed on-frame place until the guest is visible; and UiWolk must never shrink below its content or below 48 px (pin min size like the toast fix). Add a test that opens a wolk with volg on a guest in another room and asserts the rect ≥ 48×48 inside the frame and one line of text. Evidence: /tmp/v1/shots/E1-rekening-stap1.png (copy it to .fanout/scratch/godot-i1/ before /tmp is cleaned).
2. HIGH scenes/main.gd:325-334 — the mute is decided on the first input, which is the "Verder spelen ▸" tap that precedes the State.s swap at main.gd:89-90, so a saved geluid=false is not pushed into Snd. Push the saved sound setting into Snd after the save is restored (and whenever State.s changes), keep the audio unlock on the first tap. Test: headless, State with geluid=false → after start choice Snd is muted; and extend the chromium opslag probe (.fanout/scratch/godot-w3/opslag-probe.js) to assert peak 0 after reload with geluid=false.
3. LOW ui/teksten.gd:36-39 — "%d sterren" hardcoded; apply the meervoud rule from world.md §7.10 (1 ster / n sterren, and check gast/gasten, munt/munten the same way).
4. LOW toasts and cards over tap targets: "Precies! 🎉" over the Zwembad chip, the check-in card over the till. Toasts must be placed by Hits (kind "tag" or a reserved toast band that excludes the room bar and chrome); cards must not cover the object they belong to nor the till (the desk is a Kamer.balie footprint: reserve it).
5. INFO prikbord cards have no object rect: give the two board cards a rect from their own Control so dekking covers all five boot hotspots (vlakken 5 of 5).

CONTEXT AND RULES
As in I1 (ticket .fanout/tickets/godot/i1-integratie.md): you are the only worker in the main Godot tree (ten game workers run in worktrees on games/<id>/ only). Same write set. Before reporting: import (0 ERROR), tools/test.sh (0 fout), export (0 ERROR), tools/probe.js on the three profiles (7/7 steps), the extended opslag probe. No commits.

OUTPUT FORMAT
Status word first, then at most 20 lines: per finding "fixed: cause → fix", check results with counts and exit codes, contract changes, concerns.
