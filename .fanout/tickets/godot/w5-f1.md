# W5-F1 — Small fix batch for W5 (math core + runner)

FINDINGS TO FIX (from R-W2W5 and R-W3W6; ledger entries of 2026-09-09)
1. SHOULD-FIX tests/run_tests.gd:88-90 — a test file with a parse error is skipped silently (the bare --script run printed 192 goed, 0 fout with a broken tests/test_kapot.gd present; tools/test.sh caught it via stderr but architecture.md §14.3's bare command is blind). Fix: if the script fails to load or cannot instantiate, count it as a failure with the file name, and exit 1. Prove red→green with a throwaway broken file, then delete it. Update §2.4/§14.3.
2. NIT the byte-identity sweep omits Sommen.Voerkar.* and Sommen.Meubels.* (games-a.md §5.3, §7.2; demos/dierenhotel/games/meubels.js:105-106 and voerkar.js:431-434 are liftable by line range like the others). Add them to bouw_harnas.py, regenerate ref.json and the FNV fingerprints in test_sommen_kruis.gd, re-run vergelijk.py to 0 differences.

RULES
Same write set and constraints as your original ticket unless extended below; architecture.md may be edited surgically for contract fixes. Other fix workers run concurrently on disjoint files. No commits, no git add, no subagents, no blanket process kills. Before reporting run: import (0 ERROR), tools/test.sh (0 fout, or list exactly which failing tests belong to another workstream still in flight), export (0 ERROR). Report: status word first, then at most 20 lines: per finding "fixed: how" (or "not fixed: why"), check results with counts and exit codes, contract changes, concerns.
