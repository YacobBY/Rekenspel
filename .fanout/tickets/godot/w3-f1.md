# W3-F1 — Fix batch for W3 (UI shell) after the preliminary review

FINDINGS TO FIX (from R-W3W6; ledger entry of 2026-09-09)
1. BLOCKING ui/blad.gd:22 — set_anchors_preset(FULL_RECT) keeps the offsets, so the sheet root is 0×0 and every Ui.blad_open renders as a 560×24 strip at (0,0): start screen, plattegrond, kassa, brievenmuur, the day sheets. Use set_anchors_and_offsets_preset (or set offsets explicitly) and verify with get_global_rect().
2. BLOCKING ui/blad.gd:48-52 — the ScrollContainer has vertical minimum 0 inside a CenterContainer, so the panel is 24 px tall and clips everything. Give the sheet a real size policy: content-sized up to a maximum of the frame minus margins, scrolling only when taller.
3. BLOCKING consequence — because the start sheet was invisible, State.start_gekozen() was never called and every bewaar() stayed a no-op, so progress is lost each session. After fixing 1–2, prove in chromium with the repo probe (tools/probe.js, W6 is fixing its expectations concurrently; coordinate on the [probe] line names: keep yours, W6 adapts) that: boot → start sheet visible → tap "verder"/"nieuw" → play a bit → reload → the same day and stars are back.
4. SHOULD-FIX tests: every sheet test (test_ui.gd:314-327, 433-445) must assert get_global_rect() of the panel and of at least one button inside the frame, not only nodes and text.
5. SHOULD-FIX hits.gd:236,550-552 — object rects come only from spots with vlak; hotel buttons pass none, so dekking 0.0 proves nothing. In Hits.maak, when vlak is absent and a model/obj is known, derive it from World.vlak_van(model, …); make the probe's dekking_max cover every button in the room including the hotel's own. Document in architecture.md §4.3.
6. DECISION thema.gd:96 — tap_van measures the frame, giving 44 px keys on a 360 px phone. Lead decision: gate on the window's short side; keys are 48 px at exactly 360 and 44 only below 360 (art-sound-rules.md §16.5 wins over the looser reading of HOTEL.md §9). Update the test.
7. NIT ui/kamerbalk.gd:63-65 — the compact rail wraps mid-word (Recept/ie, Keuke/n, Wasse/rij) and is 176 units wide against the 98 of §16.4; use icon-only chips or autowrap off with a fixed width per §16.4, and give the frame the width back at 740×360. Measure before/after.
8. NIT ui/teksten.gd:57 — remove the unused, invented KAART_CHIP.

RULES
Same write set and constraints as your original ticket unless extended below; architecture.md may be edited surgically for contract fixes. Other fix workers run concurrently on disjoint files. No commits, no git add, no subagents, no blanket process kills. Before reporting run: import (0 ERROR), tools/test.sh (0 fout, or list exactly which failing tests belong to another workstream still in flight), export (0 ERROR). Report: status word first, then at most 20 lines: per finding "fixed: how" (or "not fixed: why"), check results with counts and exit codes, contract changes, concerns.
