# W1-F1 — Fix batch for W1 (world) after the preliminary reviews

FINDINGS TO FIX (from R-W1W4 and R-W2W5; ledger entries of 2026-09-09)
1. BLOCKING rooms.gd:400 — rename `slots_van(soort, kamer)` to the published `slots(kamer, soort)` (world.md §5.3; kamer may be empty/null meaning all rooms) and add the `World.slots` passthrough. Update every caller in your files and tests. W2 is concurrently dropping its has_method guard and calling `Rooms.slots(kamer, "bed")`; do not edit W2's files.
2. BLOCKING wereld/voor.gd:34-35 — the 💤 drop the Art.S/Art.HG factors; use the existing tested `ArtEffect.zzz_rechthoeken` (art/effect.gd:74, including the face<0 shift) instead of re-inlining. Also replace the other re-inlined helpers (world.gd:1491, voor.gd:23-25, dier.gd:14-15: pluis/pluis_maat/pluis_alfa/grondschaduw) by calls to ArtEffect so the values cannot drift.
3. BLOCKING for wave 2: ctx.wereld = World but the documented §5.3 world API is missing on World: kamers, kamer, pad, slots, slot, actief, set_food, verwijder_meubel, kamer_meubels, meubeltypen, decor_wis_alles, accessoires, accessoire_weg, and any other §5.3 name the reviewer's "…" hides — go through world.md §5.3 and architecture.md §6.3/§6.4 name by name and implement every one on World (thin passthroughs to Rooms where that is where the data lives). Add a test that asserts by reflection that every name in that list exists on World with the documented arity.
4. SHOULD-FIX games/_voorbeeld/spel.gd:21-22 — replace proefkamer/proef_blok by "receptie"/"kist"; fix tests/test_games.gd:49 accordingly (this file is W3's, but only that stale assertion: edit that one line and mention it). Write set extended with games/_voorbeeld/** and that single line.
5. SHOULD-FIX scenes/kamer.gd:105-107 — a bought bakje (model "" per world.md §1.8) must draw: key the bowl on soort == "bak" before the model == "" return, as world.js keys it on soort.
6. Floor ownership: the ArrayMesh in scenes/vloer.gd is the single floor renderer. Take the colour rule from ArtVloer.kleur (W4 is concurrently reducing art/vloer.gd to the colour table and fixing its matten Dictionary handling and deleting Art.vloer_plaat); rooms.gd:168 must call that table rather than keep a third copy. Coordinate only through the documented signature `ArtVloer.kleur(kamer_record) -> Dictionary`; if W4's version is not there yet when you finish, keep your call site ready and say so.
7. NIT world.gd:1088 off-screen eet never empties the bowl (world.md §2.9); world.gd:1353 floating swimmer re-arms zwem every tick and lacks the every-30th-tick splash. Fix both.

RULES
Same write set and constraints as your original ticket unless extended below; architecture.md may be edited surgically for contract fixes. Other fix workers run concurrently on disjoint files. No commits, no git add, no subagents, no blanket process kills. Before reporting run: import (0 ERROR), tools/test.sh (0 fout, or list exactly which failing tests belong to another workstream still in flight), export (0 ERROR). Report: status word first, then at most 20 lines: per finding "fixed: how" (or "not fixed: why"), check results with counts and exit codes, contract changes, concerns.
