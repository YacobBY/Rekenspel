# W2-F1 — Fix batch for W2 (hotel, economy, save) after the preliminary reviews

FINDINGS TO FIX (from R-W2W5 and R-W1W4; ledger entries of 2026-09-09)
1. BLOCKING state.gd:314,317 — drop the `Rooms.has_method("slots")` guard and call `Rooms.slots(kamer, "bed")` / `Rooms.slots("", "bed")` per world.md §5.3 (W1 is concurrently renaming its slots_van to exactly this signature; code against the published name, not the current file). Change the four tests that asserted a bedless hotel (test_hotel.gd:319, :357, :413, :449): give `_gasten()` every bed or expect the prio-0 bel card, whichever the spec implies. If W1's rename has not landed when you finish, report the four tests as pending-on-W1 with their expected values.
2. BLOCKING state.gd:168,177,218 — `lees()` swaps in the loaded dict before _normaliseer/_repareer and _vorm_klopt never checks rekening.hand/bank; the reviewer's file {"v":1,"s":{"rekening":{"hand":"kapot","bank":3}}} produced a SCRIPT ERROR while lees() returned true. Shape-check every field of §9 including rekening (hand, bank, and each nested type), run normalise/repair on the candidate before the swap, and make any failure fall back to the start screen with the old state untouched. Add that corrupt file and two more of your own as tests.
3. SHOULD-FIX hotel.gd:81 — herstel_wereld() calls Rooms.herstel() (base layout) so bought furniture is wiped every boot; replay s["meubels"]/s["meubelNr"] through the furniture API (world.md §1.8) as the HTML's Games.herstelInrichting does. Test: buy one furniture item, save, reload, assert it is back.
4. SHOULD-FIX hotel/rekening.gd:180 — klaar fires at once, so the 👋 bubble's guest is despawned in the same frame. Restore the 1100 ms rule from world.md §3.5 (bubble first, then the callback and World.sync), keeping the save-backed state as you have it; document in architecture.md §9 that the bill is out of the save before the delay. Also the step-2 500 ms wait (econ.js:187) at rekening.gd:78 — restore it.
5. NIT architecture.md §9 field table omits rekening: add it.
6. NIT hotel.gd:794 tik_mand — call World.mood("bouncy") after the walk resolves, so the +0.06 wandel_kans is not lost; keep ga("blij") for the arrival bounce.

RULES
Same write set and constraints as your original ticket unless extended below; architecture.md may be edited surgically for contract fixes. Other fix workers run concurrently on disjoint files. No commits, no git add, no subagents, no blanket process kills. Before reporting run: import (0 ERROR), tools/test.sh (0 fout, or list exactly which failing tests belong to another workstream still in flight), export (0 ERROR). Report: status word first, then at most 20 lines: per finding "fixed: how" (or "not fixed: why"), check results with counts and exit codes, contract changes, concerns.
