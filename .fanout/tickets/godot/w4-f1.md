# W4-F1 — Fix batch for W4 (art baker + sounds) after the preliminary review

FINDINGS TO FIX (from R-W1W4; ledger entry of 2026-09-09)
1. SHOULD-FIX Floor ownership: W1's ArrayMesh in scenes/vloer.gd is the single floor renderer. Delete `Art.vloer_plaat` (dead, 160 ms) and reduce art/vloer.gd to the colour table with the documented signature `ArtVloer.kleur(kamer_record) -> Dictionary` that accepts Rooms.Kamer records as they are (matten is a Dictionary, not an Array: the current code would crash). W1 is concurrently switching rooms.gd:168 to call it. Update architecture.md §3 accordingly.
2. SHOULD-FIX tests/gouden/wav/ does not exist though test_snd.gd:6 says the WAV oracle lives there; the sound oracle dies with /tmp. Store the 18 reference WAVs (or a compact reference: per-sound sample count, peak, RMS, and a 64-bin spectrum fingerprint if 2.6 MB is too much) under tests/gouden/ behind the existing .gdignore, and point the test at it. Reconcile the kar level (art-sound-rules.md §15.3 −29.6 dB vs snd-meta.json −31.5): state which is right by re-exporting from the HTML with /tmp/dh-snd/export.js.
3. SHOULD-FIX Browser bake cost unmeasured: §3.5 budgets ≤ 25 ms/plate in the browser. Measure the cold bake of the receptie and the tuin inside the exported wasm (print timings from the probe game or a query flag, read them from the console via Playwright with the swiftshader flags) and record the numbers in architecture.md §3.5. If a room exceeds the 300 ms slide in wasm, say so with the number and propose (do not implement) the fix.
4. NIT golden coverage is hond-only for poses: extend tests/gouden with konijn/poes/gans in lig, zwem and loop at g=4 (extractor in tests/gouden), and assert them like the others.
5. NIT architecture.md §3.6 says 64 plates; there are 63.

RULES
Same write set and constraints as your original ticket unless extended below; architecture.md may be edited surgically for contract fixes. Other fix workers run concurrently on disjoint files. No commits, no git add, no subagents, no blanket process kills. Before reporting run: import (0 ERROR), tools/test.sh (0 fout, or list exactly which failing tests belong to another workstream still in flight), export (0 ERROR). Report: status word first, then at most 20 lines: per finding "fixed: how" (or "not fixed: why"), check results with counts and exit codes, contract changes, concerns.
