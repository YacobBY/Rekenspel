# A1-F1 — Fix batch after the preliminary review of A1

TASK
Apply the review findings below to the architecture document and the skeleton, keep every existing check green, and re-run the full verification (§14). Same write set as A1.

FINDINGS TO FIX
1. BLOCKING `autoload/hits.gd:212-221`: `_kies_plek` returns early for `midden` and `rand` and never writes `_bezet`, so buttons land on fixed cards (reproduced at 1000×648: 3 of 4 `boven` buttons on a `vast, prio 14` card, 1728 px² overlap each). §4.3 promises that a fixed card reserves every cell it covers. Also close the escape paths at `:238` (`_klem` on the aim point) and in `_cel`→`_klem` clamping: a clamped placement must still be checked against `_bezet` and against every object's box, and fall back to the next free band cell, never to an overlapping one. Add a test that reproduces the reviewer's case (fixed midden card plus four boven buttons at 1000×648, 360×740 and 768×1024) and asserts 0 % coverage for every button against every card and object.
2. BLOCKING `tests/run_tests.gd:60`: a runtime `SCRIPT ERROR` inside a test currently prints `ok` and the suite exits 0. Make the runner fail (non-zero exit, `fout` count incremented) on any script error, push_error, or unhandled failure during a test; the simplest robust way is to capture stderr in the wrapper script the CI and tools use AND to check `Engine`-level error counts if available; document the chosen mechanism in §2.4 and §14.3. Prove it with a deliberately broken throwaway test (then delete it) and paste the failing exit code in the report.
3. BLOCKING `autoload/world.gd`: add stubs, with the `# Wn` marker pattern used elsewhere, for the whole documented wave-2 interface: `decor`, `decor_weg`, `decor_lijst`, `decor_plek`, `decor_wis_eigenaar`, `ga`, `reis`, `slaap`, `feed`, `solo`, `mood`, `pose` — signatures exactly as in §6.4 and §12.2, bodies that push a clear warning naming ticket W1 and return a sensible default.
4. SHOULD-FIX §6.5 (the wekker worked example): make it implementable as written. Remove or implement the `maat` key of `Hits.maak` (decide: if a button can have a size, hits.gd must read it and the grid must respect it); use the qualified model name `wekker_klok` per §13 Q-X1-11 and make `Art.registreer_model` reject bare names that collide with world models; make `ctx.wereld.decor` real via finding 3. Walk the example again call by call against the skeleton after the fix.
5. SHOULD-FIX write-set collision: the placeholder models `blokdier`/`blokje` live in `art.gd` (W4's file) while §11 says W1 deletes them, and `tests/test_skelet.gd` has no owner and asserts on `blokje`/`proefkamer`. Resolve by moving the placeholder models into a file W1 owns (e.g. `scenes/proef_modellen.gd` registered from `scenes/kamer.gd`) and giving `tests/test_skelet.gd` to W1 in §12.1, or by deleting the skeleton test and covering its assertions in the W1/W3 test lists. Use one model name consistently.
6. SHOULD-FIX `tests/test_hits.gd`: extend with the reviewer's adversarial shapes (top-edge object, 360×740, 10 competing buttons, and the fixed-card case from finding 1).
7. NIT: give W3 a number for the compact landscape shell (`KADER_ONDER` 132 within `KADER_MIN` 200 leaves 68 units; state what fits there or change the constants); remove the empty `ui/` directory or put a README stub in it; let `_voorbeeld` exercise `somkaart`, `wolk` and `decor` so the vertical slice covers the contract wave 2 relies on.

CONTEXT
Review brief: see the ledger entry "R-A1" in .fanout/ledger.md (section "Run dierenhotel-godot"). Wave-1 tickets are written in .fanout/tickets/godot/w1-w6 and reference §12.1; if your fix changes an owner or a write set, update §12.1 and tell me in the report so I can adjust the tickets.

CONSTRAINTS AND RULES
As in A1: write set `.fanout/specs/godot/architecture.md` and `godot/dierenhotel/**`; no subagents; no commits; no blanket kills.

OUTPUT FORMAT
Status word first, then at most 20 lines: per finding one line "fixed: how", the verification results (import, tests with count, export, the broken-test proof exit code, browser probe), any §12.1 write-set changes, concerns.
