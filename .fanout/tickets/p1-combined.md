TICKET P1 — Preparation for five new Dierenhotel minigames: rooms, wishes, model hook, animal movement with completion, accessories, stubs, docs.

TASK
Five games will be written next by five parallel workers, each confined to its own file games/<id>.js plus its own test suite. Your job is to add everything SHARED that they need so they never touch a shared file. The games and their needs are specified in /home/pc/Documents/xnw/rkn/.fanout/specs/dierenhotel-golf3-games.md (read it fully; the "Vereist van P1" lines and the "Gedeelde eisen" section are your requirements list). Summary of the games: G1 zwembad (swimming lengths, room `zwembad`), G2 wekker (wall clock in the gang), G3 hinkel (stepping-stone number line in the tuin), G4 was (laundry sorting into bar-chart crates, room `wasserij`), G5 kraam (souvenir stall in the tuin, coins, accessories worn by the animal).

EXPECTED OUTCOME (all in demos/dierenhotel unless noted)
1. Rooms: new entries `zwembad` (reached by a door from `tuin`, reciprocal door) and `wasserij` (door from `keuken`, reciprocal) in rooms.js RUIMTES, with icon (🏊, 🧺), name, floor colour (zwembad: a water-blue rectangular pool area inset in the floor via Rooms.vloerKleur or a decor model, with a tiled deck around it; wasserij: plain), wand, doors, empty `slots`, `decor` with only the door frames and one or two fixed props; a `KAART` cell for each in hotel.js so both appear on the map and the room bar; `RUST` entries in world.js for the voerkar; `loop` factor consistent with the other rooms. Pick sizes that work with the per-room scale introduced by commit 710003e (read HOTEL.md §1 and world.js maatVan first): zwembad long along x (suggestion 120×80), wasserij about 100×90. The zwembad pool must span most of the room's x extent so a swim of L metres maps to pool voxels linearly (expose `Rooms.get('zwembad').bad = {x0, x1, z0, z1}` in voxels). Tuin: add `zones: {hinkel: {x0,x1,z0,z1}, kraam: {x0,x1,z0,z1}}` reserving two disjoint floor areas (hinkel along the back edge, kraam along a side edge) that stay clear of the existing tuin decor and of the doors, and document them.
2. Wishes: add 🏊 `zwemmen` and 🎁 `souvenir` as guest wishes: state.js BEHOEFTE (data only; the frozen math functions koekjesSom, vergelijk, planOplossingen, sommen.*, antwoord1/antwoord2 must remain byte-identical), hotel.js WENSWOORD (🏊 zwemmen, 🎁 souvenir), plekVanBehoefte (zwemmen → zwembad start edge; souvenir → the tuin kraam zone), behoefteKlaar, and the morning assignment in morgen(): a wish type is only handed out when a game that fulfils it is registered — generalise the existing badMogelijk (hotel.js ~628-631, hardcoded Games.get('tobbe')) into a registry-driven check, e.g. Games.register accepts `wens: 'zwemmen'` and hotel asks Games.wensMogelijk(type). Keep the distribution sensible (no more than one guest per day with the same new wish while N is small; existing behaviour for 🍪 🛏 🛁 🧶 unchanged).
3. Model hook: `Rooms.registerModel(naam, fn)` where fn(params) returns the same voxel list shape as the entries of the private MODEL map (rooms.js ~282-293) and `model(naam, params)` calls it; decor entries and a new runtime API `ctx.wereld.decor(kamerId, {id, model, x, z, params, hoog?})` (add/update by id) and `ctx.wereld.decorWeg(kamerId, id)` let a game place and re-render its own voxel objects (clock hands at a given time, laundry stacks of n blocks, stones with a number, pool flag at L metres) with proper cache invalidation (World.vuil / plate cache) and draw-order (objects inside the room box sort with the furniture). Decor added at runtime by a game is NOT persisted (games rebuild from ctx.data() at start); say so in the docs.
4. Movement with completion: `ctx.wereld.loopNaar(id, x, z, {pose, tempo}) → Promise` resolving when the animal arrives (or rejects/resolves false if the route is cancelled by another command), and `ctx.wereld.stappen(id, [{x,z}, …], {pose, tempo, perStap?}) → Promise` walking the points in order with an optional callback per reached point; pose options must include the existing ones plus new `zwem` (body bobbing at water level, lift so the belly sits at the pool surface, small splash voxels optional) and `spring` (an arc hop between consecutive points: lift follows a parabola, landing with a tiny squash). Implement in world.js Dier/World and expose via games/registry.js; reduced-motion mode must resolve the promise immediately after teleporting. Also expose `ctx.wereld.pose(id, naam, duur?)` if not already covered by zet.
5. Accessories: art.js draws `g.accessoires` (array of ids among `hoedje`, `sjaaltje`, `bal`) on the animal (hat on the head voxels, scarf around the neck, ball as a separate small model next to the animal) for all four species and in every pose including `lig`; stored on the guest record so it survives reload (state.js guest schema + save v7 payload; no version bump needed if the field is optional and defaults to []). Expose `ctx.wereld.accessoire(id, naam)` to add one.
6. Sounds: add `plons` (soft splash), `au` (a soft, short, non-alarming bump), `tik-tak`/`klok` (a soft clock chime) and `hup` (hop) to snd.js in the existing style (Web Audio, no files).
7. Stubs and wiring: create games/zwembad.js, games/wekker.js, games/hinkel.js, games/was.js, games/kraam.js each containing only a header comment ("wordt gevuld door ticket G<n>") so index.html can already reference them (add the five script tags after the existing games, before game.js); add five rows to .fanout/scratch/dierenhotel/alles.sh (`zwembad-port|node zwembad/spel.js` etc. — for port and land as the existing rows do; missing scripts print SKIP).
8. Docs: GAMES-API.md gains a section for each new API (registerModel/decor, loopNaar/stappen/poses, accessoire, wens registration, rooms zwembad/wasserij with their `bad`/`zones` data, sounds) and loses the stale claims (the "placeholders" text at ~:49-50; the comment in games/tobbe.js:39-41 that says there is no ctx function to tick a wish — fix that comment too). HOTEL.md §1 room list updated.
9. Tests: a suite .fanout/scratch/dierenhotel/p1.js (≥40 assertions, port + land) covering: both rooms reachable via doors and the room bar, map cells unique, zwembad `bad` inside the room, tuin zones disjoint and inside the floor and clear of decor footprints; registerModel + decor add/update/remove renders without console errors and invalidates the plate; loopNaar resolves on arrival (normal and reduced motion) and resolves false when interrupted; stappen visits all points in order with pose `spring` and `zwem` and lift returns to 0 afterwards; a wish 🏊 is only assigned when a game with wens:'zwemmen' is registered (register a dummy in the test) and is shown as "🏊 zwemmen"; accessories drawn (pixel diff of the animal with and without `hoedje`) and persisted across reload; smoke, l1, k1, loop, w1, hotspots, plugin, tobbe, bedden suites still green.

CONTEXT
- Repo /home/pc/Documents/xnw/rkn; you work in an isolated git worktree branched from main at b0f14c0 (rooms already 1.5×, guests sleep on beds). Vanilla JS, offline, no build. Playwright: /home/pc/work/ergomouse/node_modules/playwright; harness patterns in .fanout/scratch/dierenhotel/ (k1.js, slaap.js, tobbe/spel.js). .fanout/scratch is gitignored: write your suite in your worktree's copy and ALSO copy p1.js (and the modified alles.sh) to /home/pc/Documents/xnw/rkn/.fanout/scratch/dierenhotel/ at the end.
- API facts from a scout (verify): Games.register({id,naam,kamer,hotspot,unlock,start,stop,taak}) registry.js:27-34; ctx built in registry.js:143-258 (wereld 152-208, hotspots 211-224, data() 230, ui 238, econ/snd 239-240, taakKlaar 243-253); start/stop registry.js:260-304; scripts index.html:54-71; task chips hotel.js:648-660/694-759; wishes: BEHOEFTE state.js:50-55, WENSWOORD hotel.js:29, plekVanBehoefte hotel.js:67-83, behoefteKlaar hotel.js:98-102, morgen hotel.js:951-952, badMogelijk hotel.js:628-631; rooms schema rooms.js:295-304, RUIMTES 305-420, MODEL 282-293, vloerKleur 691, vrij grid 455-475, KAART hotel.js:42-43, RUST world.js:876; Dier.ga/zet world.js:100-111 (`na` is a pose string, no callback), World.ga :980-987, reisNaar :990-1009, poses handled world.js:275-345; art.js poses/oog; snd.js:61-97 sounds. Line numbers moved slightly after 710003e/b0f14c0 — re-locate.
- Hard design rules (HOTEL.md §9): math in-world, ≥48 px targets, portrait+landscape, never punishing, animals soft; hotspot cap 16 per layer.
- A reviewer is concurrently grading commit 710003e in the main tree; a fix batch may later touch world.js maatVan/camera, hits.js, bedden.js, state.js migration. Keep your hunks away from the camera/scale code and the migration code so the merge stays clean; add new functions rather than rewriting existing ones.

CONSTRAINTS
- Frozen math byte-identical (prove with md5 of the function bodies or `git diff -U0` showing only BEHOEFTE/schema hunks in state.js). No new dependencies. No blanket pkill. You do not spawn agents. Do not commit (the lead merges and commits).
- Keep the five stub files free of any registration so nothing changes in play until the games land.

MUST DO
- Read the spec file first; then rooms.js, world.js Dier class, registry.js, hotel.js wish code, art.js pose code, snd.js.
- Run node --check on every changed file; run the listed suites (report exact pass/fail per suite, against YOUR worktree copy — check each suite's URL/DH_SRC handling; several hard-code the main-tree path) and the new p1.js.
- Screenshots p1-zwembad-{420x860,860x420}.png, p1-wasserij-{420x860,860x420}.png, p1-tuin-zones.png (zones drawn as an overlay in the test only), p1-accessoires.png (four species wearing hoedje+sjaaltje with bal, in `lig` and standing).

MUST NOT
- Implement any of the five games' logic. Change camera/scale code, the save migration, existing game files (except the tobbe.js comment), ui.js wording, existing sounds. Touch demos/waggletail or demos/silverhoof.

WRITE SET
demos/dierenhotel/rooms.js, world.js, hotel.js, state.js (BEHOEFTE + guest schema only), games/registry.js, art.js, snd.js, index.html, style.css (room bar icons only if needed), games/{zwembad,wekker,hinkel,was,kraam}.js (stubs), games/tobbe.js (comment :39-41 only), GAMES-API.md, HOTEL.md §1, .fanout/scratch/dierenhotel/p1.js and alles.sh (copied to main scratch too).

OUTPUT FORMAT
First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then ≤40 lines: per item 1–9 what exists now (file:line, exact API signatures for the game workers), room dims chosen and the `bad`/`zones` values, suite results, screenshot paths, worktree path/branch, frozen-math proof, concerns.