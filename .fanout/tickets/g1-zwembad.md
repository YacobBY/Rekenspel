TICKET G1 — Minigame "Zwembad" (id `zwembad`, room `zwembad`) for the Dierenhotel demo. Read /home/pc/Documents/xnw/rkn/.fanout/tickets/_gemeenschappelijk-g.md first; it is part of this ticket.

TASK
Replace the stub demos/dierenhotel/games/zwembad.js with the full game. This is the owner's own design (spec G1 in .fanout/specs/dierenhotel-golf3-games.md): a pool of L metres, the animal swims exactly one metre per stroke, the child picks from four distances how far to swim (at most M metres per pick); too short means the animal simply stops and the child picks the rest; too far means the animal bumps the far wall, says a short soft "Au!", rubs its head for at most 1,5 s and is then happy at the far side; exactly right means "✅ Precies aan de overkant!".

EXPECTED OUTCOME
1. Games.register({id:'zwembad', naam:'Zwembad', kamer:'zwembad', hotspot on the pool start edge / ladder, unlock(N) true for N ≥ 1, wens:'zwemmen', start(ctx), stop(), taak → chip "🏊 Zwemles" whenever a guest carries the 🏊 wish}). Registering makes the hotel hand out the 🏊 wish (registry-driven, ticket P1d): confirm in the suite that the wish appears within a few mornings.
2. World: the pool rectangle Rooms.get('zwembad').bad = {x0,x1,z0,z1} spans L metres along x. Use runtime decor (ctx.wereld.decor with your own Rooms.registerModel models, registered inside your file) for: metre marks on the deck edge (0, 5, 10, … when L ≤ 20, else 0, 10, 20, …), a flag or line at exactly L metres, and a small position tag on the animal showing the metres swum ("30 m", via getalTag or an equivalent). Clear your decor in stop().
3. Turn: the guest with 🏊 waits on the deck at Rooms.get('zwembad').dek.start (the hotel sends it there; that spot is in front of the water line, not next to it). When the turn starts the game moves the animal itself (ctx.wereld.loopNaar) to the water entry at x = x0, z = the swim lane (z0 + z1) / 2 with a Snd.plons, then p = 0. Only the game ever places an animal inside the pool rectangle; when the turn ends (any outcome) the animal climbs out onto the deck at dek.over (or dek.start) before control returns to the hotel, so idle wandering never starts from inside the water. Card: sentence "🏊 Het bad is 43 meter lang", second line "Max 30 meter per keer", no sum yet, four choice buttons like "🏊 30 m", "🏊 43 m", "🏊 13 m", "🏊 20 m". The child taps one; the animal swims that many strokes with pose 'zwem' (ctx.wereld.stappen or loopNaar with tempo; one Snd.plons per few strokes, not per stroke when > 10) and the tag updates. Rules: r = L − p; correct = r when r ≤ M, else M. Too short: stops, new card "🏊 Boef is bij 30 meter" / "Nog hoeveel meter?" with the sum "43 − 30 =" and four new choices. Too far: swims to the wall, bump (Snd.au, wolk "Au!", short head-rub or shake pose ≤ 1,5 s, then a happy pose), no star lost, the turn ends as reached. Exactly: "✅ Precies aan de overkant!", Snd success, behoefteKlaar for the wish, taakKlaar + star. The animal climbs out at the far edge and the wish is gone. One length per guest with the wish.
4. Numbers per band (all generated in your file from band and N): groep 3: L ≤ 20, M = 10, whole metres, every choice ≤ 20. Groep 4: L 21–50, M = 30 (example 43 m: first 30, then 13). Groep 5: L 31–100, M = 30 or 40, remainders that cross a ten (76 − 40 = 36). Distractors: r ± 1..3, r + 10, L itself, M itself; never negative, never duplicated, exactly one correct choice; the correct answer must not always sit in the same button position.
5. Hotspot without a swimming guest: if no guest has the wish, the hotspot invites the nearest guest in the zwembad room (or the first guest anywhere, walked in) for a practice length with the same flow but without the wish completion; never a dead hotspot. Guests never wander into the water (the room's wander grid already excludes it; do not place a guest inside the pool except while swimming).
6. Suite .fanout/scratch/dierenhotel/zwembad/spel.js per the common block, including: metres → x mapping is linear (tag at 30 m sits at x0 + 30/L·(x1−x0) ± 1 voxel); too short / too far / exact each asserted with their consequences; band ceilings for L, M and every choice; reload after the first leg restores L, p and the guest; wish handed out after registration; screenshots zwembad-port-kaart.png, zwembad-port-zwemt.png, zwembad-port-au.png, zwembad-land-kaart.png.

MUST NOT
Edit any file outside games/zwembad.js and your scratch folder (no rooms.js, world.js, hotel.js, state.js, registry.js, snd.js, css). No timers as pressure, no red, no repeated turn after a bump.

WRITE SET
demos/dierenhotel/games/zwembad.js; .fanout/scratch/dierenhotel/zwembad/**.

OUTPUT FORMAT
See the common block.
