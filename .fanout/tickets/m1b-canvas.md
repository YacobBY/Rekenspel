TICKET M1b — Mobile world canvas: frame sizing, layout bus, density cap and idle cost for the Dierenhotel demo.

TASK
The owner requires the game to work on real phones. A read-only scout measured demos/dierenhotel with Playwright device emulation (iPhone 13 dpr 3, Pixel 5, 360×740) and found the world canvas layer (world.js, art.js) wastes memory and CPU and relayouts unstably. Fix it. A parallel ticket M1a fixes the shell (style.css, index.html, ui.js, snd.js, game.js) and will use `World.onKader(fn)` when it exists: do not touch M1a's files.

EXPECTED OUTCOME (verify each scout claim yourself first)
1. Layout bus: `World.onKader(fn) → unsubscribe` fed by one ResizeObserver on #world (fallback: window resize + orientationchange) with a single ~120 ms debounce; fn receives the frame rectangle and the current per-room scale. world.js's own resize handling (~1368 immediate vuil, ~491-527 frame maths) goes through the same path. Keep the old window listeners in other files working until M1c migrates them (do not edit hotel.js, games, ui.js).
2. Stable frame height: ruimteHoogte / ruimVoorKader / kaderHoogte (world.js ~491-527) derive the frame from window.innerHeight and clamp to 200 px. Use a stable measure (visualViewport height or 100svh via a probe element, orientation- and width-driven) and ignore height-only deltas < 120 px (the iOS URL bar) so the frame does not jump while a child drags; lift the 200 px clamp to "what fits" once M1a's compact landscape chrome frees space (compute from available height, minimum 200, no upper clamp below the room's natural height).
3. Density cap (P1, world.js ~514): dicht = max(d, ng/q) is uncapped; on iPhone 13 it reaches ~4 (canvas 1480×1662, 9.6 MB, full-frame drawImage of a 6.4 MB plate ~31×/s). Cap dicht at 2 when devicePixelRatio > 2 and innerWidth < 500 (g stays an integer; the CSS downscale is invisible at 3× screens); keep dpr-based density elsewhere; document the rule in HOTEL.md §1 if a sentence exists about density (else in your api note).
4. Redraw only when something changed: set vuil only when a dier, bak, decor, camera or reis actually changed; measure the frame only from the ResizeObserver callback (no per-frame getBoundingClientRect); pause the world loop and the art.js sprite loop while document.hidden; the art.js 12 fps loop (~851-870) runs only while sprites exist (start from koppel(), stop when the last sprite disconnects) and its MutationObserver (~946) is scoped to #sheet / #scene instead of the whole body. Reduced-motion mode must still render on demand.
5. hits.js is NOT in your write set any more: ticket H1 (separate worktree) owns hits.js (placement, short-frame evasion, onWeg hook). If world.js must expose something for it (e.g. the object's top edge in the mik box), add it additively and name it in your report.

6. Perf probe .fanout/scratch/dierenhotel/mobiel-perf.js (Playwright chromium, devices['iPhone 13'] and 360×740 dpr 3, hasTouch/isMobile): CDP Emulation.setCPUThrottlingRate 4, a 3 s probe wrapping requestAnimationFrame and CanvasRenderingContext2D.drawImage, reporting p95 frame time, drawImage calls per frame idle vs. walking guest, canvas backing store bytes, count of window resize listeners (page.addInitScript), and a height-only +80 px viewport change asserting no frame relayout; acceptance: p95 ≤ 33 ms at 4× throttle idle, backing store ≤ 6 MB on the phone profiles, no layout jump on the +80 px change, rotation relayouts exactly once; final line in the alles.sh format; add the row `mobiel-perf|node mobiel-perf.js` to alles.sh (rows only; M1a also adds rows — append yours at the end).
7. Regressions against your worktree: k1.js (191, six viewports), l1.js, slaap.js (124/0), smoke, loop (port + land), w1 (port + land), hotspots, p1b.js (runtime decor, if merged into your base — check `git log`), tobbe (port + land). Report every number.

CONTEXT
Repo /home/pc/Documents/xnw/rkn; isolated git worktree branched from main (harness-created; absolute paths). Vanilla JS, offline file://. Read HOTEL.md §1 (per-room scale q, integer g, density) and the K1 notes in world.js maatVan before changing density. The scratch dir is gitignored: copy harnesses into your worktree's scratch (DH_SRC / DH_OUT where supported) and copy mobiel-perf.js, alles.sh rows and screenshots to /home/pc/Documents/xnw/rkn/.fanout/scratch/dierenhotel/ at the end. Kill only browsers you started. Long shell heredocs are rejected by the sandbox: use Write/Edit. No dependencies, no agents, no commit. Write .fanout/specs/api-m1b.md (onKader signature, density rule, pause semantics) for the docs ticket.

MUST NOT
Edit hits.js (ticket H1), style.css, index.html, ui.js, snd.js, game.js, hotel.js, econ.js, rooms.js, state.js, registry.js or any game file. Do not change room sizes, camera framing rules other than the clamp, the plate cache design (4 slots) or the sleep logic.

WRITE SET
demos/dierenhotel/world.js, demos/dierenhotel/art.js, HOTEL.md §1 (one sentence on density if applicable), .fanout/scratch/dierenhotel/mobiel-perf.js, .fanout/scratch/dierenhotel/alles.sh (one row), .fanout/specs/api-m1b.md.

OUTPUT FORMAT
First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED; then ≤ 30 lines: per item file:line with measured before/after numbers (density, backing store, p95, drawImage per frame, listener counts), perf probe results per profile, regression numbers, worktree path/branch, concerns for M1a/M1c.
