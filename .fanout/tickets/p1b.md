TICKET P1b — Model registration hook and runtime decor API for Dierenhotel minigames.

TASK
Today voxel shapes live in the private MODEL map in demos/dierenhotel/rooms.js (~282-293 at 25a4b52; `model()` silently falls back to pPol for unknown names) and decor is static per room. Five upcoming games (see /home/pc/Documents/xnw/rkn/.fanout/specs/dierenhotel-golf3-games.md: a wall clock whose hands turn, laundry crates whose block stacks grow, numbered stepping stones, a pool flag at L metres, a stall with wares) need to define their own voxel models in their own game file and place/update/remove them at runtime. Build that.

EXPECTED OUTCOME
1. `Rooms.registerModel(naam, fn)` where `fn(params)` returns the same voxel-list shape as MODEL entries; `model(naam, params)` (and every internal caller) passes `params` through; built-in entries keep working unchanged. Decor entries in RUIMTES may carry `params`.
2. Runtime decor API exposed to games through the registry extension hook (push a function onto `window.CTX_UITBREIDINGEN` from world.js or rooms.js; do NOT edit games/registry.js — read the comment at the end of ctxVoor in games/registry.js): `ctx.wereld.decor(kamerId, {id, model, x, z, params, hoog?, rot?})` adds or updates by id (re-render when params change), `ctx.wereld.decorWeg(kamerId, id)`, `ctx.wereld.decorLijst(kamerId)`. Runtime decor is owned by the game (`door` = the game id) and cleared automatically when that game stops (hook into whatever cleanup path exists, e.g. Hits.wisEigenaar-style, without editing registry.js — a World-level `decorWisEigenaar(eigen)` called from the extension is fine if you can find a stop signal; otherwise document that games must clear their own decor in stop()). Runtime decor is not persisted.
3. Rendering in world.js: runtime decor draws in the room with correct isometric draw order relative to furniture and animals (an object behind a bed is occluded by the bed, an animal in front of a stone occludes it) and proper cache invalidation: if static decor is baked into the cached room plate (plate cache keyed id|g, 4 slots), runtime decor must either render dynamically each frame (preferred if cheap) or invalidate the plate on change without thrashing. Must also work with the per-room scale of commit 710003e (read HOTEL.md §1 and world.js maatVan). No per-frame allocations in the hot path beyond what furniture drawing already does.
4. Suite .fanout/scratch/dierenhotel/p1b.js (port 420×860 + land 860×420, ≥30 assertions): register a parametrised model (e.g. a stack of n blocks), add it in kamer1 and in tuin, pixel-diff proves it is drawn; update params → pixels change; remove → pixels back; draw order test (object placed behind a bed is partially hidden; object in front of a sleeping/standing guest is drawn over it); plate cache slot count unchanged after 20 updates; no console errors; a game's decor disappears when Games.start() switches to another game (use an existing game like tobbe as the second game); screenshots p1b-decor-{420x860,860x420}.png. Existing suites smoke, l1, k1, loop (port+land), slaap.js, tobbe (port+land) stay green — run them against your worktree (override their hard-coded main-tree URL locally; do not change their defaults).
5. .fanout/specs/api-p1b.md in your worktree: precise signatures, voxel-list shape expected from a model fn, coordinate conventions (x,z in room voxels, y up, origin), draw-order rules, ownership/cleanup, limits.

CONTEXT
Repo /home/pc/Documents/xnw/rkn; isolated git worktree branched from main at af8ab32. Vanilla JS, offline, no build. Playwright at /home/pc/work/ergomouse/node_modules/playwright; harness patterns in .fanout/scratch/dierenhotel/ (k1.js, slaap.js, tobbe/spel.js). .fanout/scratch is gitignored: write your suite under your worktree's .fanout/scratch/dierenhotel/ and copy it plus screenshots to /home/pc/Documents/xnw/rkn/.fanout/scratch/dierenhotel/ at the end. Do not edit GAMES-API.md. Hard rules: HOTEL.md §9; frozen math in state.js untouched. No new dependencies; no blanket pkill; you do not spawn agents; do not commit. Parallel tickets, each in its own worktree, edit: rooms.js RUIMTES table/vloerKleur (new rooms), world.js Dier class (movement with promises, poses zwem/spring), world.js RUST line, hotel.js wish code, art.js accessories, state.js. Keep your hunks localized (rooms.js MODEL/model region + export; world.js drawing/cache regions only — not the Dier class, not maatVan/camera, not RUST) so merges stay clean; add new functions rather than rewriting existing ones.

MUST NOT
Change existing models' output, room dimensions, camera/scale code, the Dier class, any game, state.js, registry.js.

WRITE SET
demos/dierenhotel/rooms.js (MODEL/model region + registerModel export), demos/dierenhotel/world.js (drawing/cache/extension-hook regions), .fanout/specs/api-p1b.md, scratch suite + screenshots.

OUTPUT FORMAT
First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED; then ≤30 lines: signatures, rendering/cache design and its per-frame cost, file:line hunks, suite numbers, screenshots, worktree path/branch, concerns.