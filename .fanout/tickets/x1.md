TICKET X1 — Fix the four failing landscape assertions of the sleutels (key board) minigame in Dierenhotel.

TASK
The runner .fanout/scratch/dierenhotel/alles.sh reports the suite `sleutels-land` (`node sleutels/spel.js land`, viewport 1000×640 dpr 2 or similar — check the suite) at 108 PASS / 4 FAIL; the same four fail at commit 25a4b52 (evidence: .fanout/scratch/dierenhotel/k1-sleutels-land-HEAD.txt and alles-out/sleutels-land.txt). They concern the hook row and the sum card in the ~200 px-high landscape world frame: the card is already clamped to the top edge and still collides/overflows. Make the sleutels tableau lay out correctly in landscape without regressing portrait.

EXPECTED OUTCOME
- `node sleutels/spel.js land` → 0 FAIL (all 112) and `node sleutels/spel.js` (portrait) still 112/0, against your worktree. If an assertion itself encodes an impossible expectation for a 200 px frame, say so with the numbers and propose the honest guarantee instead of loosening silently — but first try a real layout fix: e.g. in a short frame put the sum card beside the board instead of above it, lower the hook row (KAART_HOOG/HAAK_Y fractions in games/sleutels.js ~73-90 since commit 710003e, expressed as fractions of r.w via vox(f)), or shrink the key chips to the 44 px minimum allowed below 360 px frames only if the frame is that small.
- HOTEL.md §9 rules hold: the card never covers the guest or the hooks, one sentence above the sum, ≥48 px targets (44 px only on frames narrower than 360 px), pictogram with its word.
- Screenshots x1-sleutels-land-{voor,na}.png (the landscape tableau before and after) and x1-sleutels-port-na.png.
- Also run the other suites that touch this game: hotspots, regels, w1 (port+land), loop (port+land) — still green against your worktree.

CONTEXT
Repo /home/pc/Documents/xnw/rkn; isolated git worktree branched from main at af8ab32 (rooms 1.5× with per-room scale since 710003e: read HOTEL.md §1; positions in sleutels.js are fractions of r.w/r.d). Vanilla JS, offline, no build. Playwright at /home/pc/work/ergomouse/node_modules/playwright. .fanout/scratch is gitignored: run the existing suites from your worktree's copy (override their hard-coded main-tree URL locally; do not change their defaults) and copy screenshots to /home/pc/Documents/xnw/rkn/.fanout/scratch/dierenhotel/ at the end. No new dependencies; no blanket pkill; you do not spawn agents; do not commit. Parallel tickets edit rooms.js, world.js, hotel.js, state.js, art.js, snd.js, index.html in their own worktrees — do not touch those.

MUST NOT
Change the game's math, wording, hotspot API, ui.js, hits.js, world.js, rooms.js. Do not loosen an assertion without stating the numbers and the reason in your report.

WRITE SET
demos/dierenhotel/games/sleutels.js, .fanout/scratch/dierenhotel/sleutels/* (suite adjustments only with justification), screenshots.

OUTPUT FORMAT
First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED; then ≤20 lines: what the four failures were (numbers), the layout change (file:line), suite numbers port/land and the others, screenshot paths, worktree path/branch, concerns.